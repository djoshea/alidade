{
  "name": "alidade-monorepo",
  "private": true,
  "version": "0.0.2",
  "description": "alidade monorepo root — not a publishable package.",
  "packageManager": "pnpm@11.1.3",
  "scripts": {
    "build": "pnpm -r --filter='./packages/*' run build",
    "typecheck": "pnpm -r run typecheck"
  }
}
